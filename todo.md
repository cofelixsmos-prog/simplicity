# to do

 add the cookie management, "do you accept cookies" and, cookie settings in nav bar -> cookie management
we need to add theme as a cookie (browser side) auto apply, auto save

URGENT: update sitemap.xml to have all the new files.